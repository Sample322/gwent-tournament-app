# 🎮 GWENT Tournament App

Турнирное приложение для карточной игры Gwent с поддержкой Telegram Mini Apps.

## ✨ Возможности

- ✅ Создание турнирных лобби с уникальным кодом
- ✅ Два игрока на лобби
- ✅ Форматы Bo3 (3 фракции) и Bo5 (4 фракции)
- ✅ Выбор фракций с таймером
- ✅ Фаза банов
- ✅ Автоматический coin flip для определения первого хода
- ✅ Real-time синхронизация через WebSocket
- ✅ Интеграция с Telegram Mini Apps
- ✅ Адаптивный дизайн для мобильных устройств

## 🛠 Технологии

- **Backend**: Node.js, Express, Socket.IO
- **Database**: PostgreSQL (Timeweb Cloud)
- **ORM**: Sequelize
- **Frontend**: Vanilla JS, CSS3
- **Deploy**: Docker, Timeweb Cloud App Platform

---

## 📋 Полная инструкция по деплою на Timeweb Cloud

### Шаг 1: Создание базы данных PostgreSQL

1. Войдите в [Timeweb Cloud](https://timeweb.cloud)
2. Перейдите в раздел **"Базы данных"** → **"Создать базу данных"**
3. Выберите **PostgreSQL 16**
4. Настройте параметры:
   - **Название**: `gwent-db` (или любое другое)
   - **Тариф**: Минимальный подойдёт для начала
   - **Регион**: Выберите ближайший
5. Нажмите **"Создать"** и дождитесь создания (1-2 минуты)
6. После создания перейдите в **настройки базы данных** и скопируйте:
   - **Хост** (например: `rc1a-xxx.mdb.yandexcloud.net`)
   - **Порт** (обычно `5432`)
   - **Имя базы** (обычно `default_db`)
   - **Пользователь** (обычно `gen_user`)
   - **Пароль** (сгенерированный)

### Шаг 2: Подготовка репозитория

1. Клонируйте этот проект или загрузите файлы в ваш Git-репозиторий (GitHub, GitLab и т.д.)

2. **ВАЖНО**: Удалите старый `package-lock.json` если он есть:
   ```bash
   rm package-lock.json
   ```

3. Сгенерируйте новый lock-файл локально:
   ```bash
   npm install
   ```

4. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Update to PostgreSQL, fix package-lock.json"
   git push
   ```

### Шаг 3: Создание приложения в Timeweb Cloud

1. Перейдите в раздел **"Apps"** → **"Создать приложение"**
2. Выберите **"Docker"** как тип деплоя
3. Подключите ваш Git-репозиторий:
   - Выберите провайдер (GitHub/GitLab)
   - Авторизуйте доступ
   - Выберите репозиторий с проектом
4. Укажите путь к Dockerfile: `./Dockerfile`
5. Выберите тариф (рекомендуется минимум 512MB RAM)

### Шаг 4: Настройка переменных окружения ⚙️

**Это самый важный шаг!** В разделе **"Переменные окружения"** добавьте:

| Переменная | Значение | Описание |
|------------|----------|----------|
| `NODE_ENV` | `production` | Режим продакшена |
| `PORT` | `3000` | Порт приложения |
| `HOST` | `0.0.0.0` | Хост сервера |
| `DATABASE_URL` | `postgresql://gen_user:PASSWORD@HOST:5432/default_db` | Строка подключения к PostgreSQL |
| `DATABASE_SSL` | `true` | SSL для Timeweb Cloud |
| `CORS_ORIGIN` | `*` | CORS (или укажите домен) |
| `ADMIN_API_KEY` | `ваш-секретный-ключ` | Ключ для админ API |

#### Как составить DATABASE_URL:

```
postgresql://ИМЯ_ПОЛЬЗОВАТЕЛЯ:ПАРОЛЬ@ХОСТ:ПОРТ/ИМЯ_БАЗЫ
```

**Пример:**
```
postgresql://gen_user:MySecurePass123@rc1a-xxx.mdb.yandexcloud.net:5432/default_db
```

#### Как сгенерировать ADMIN_API_KEY:

```bash
# Linux/Mac
openssl rand -hex 32

# Или онлайн генератор паролей
```

### Шаг 5: Деплой

1. Нажмите **"Задеплоить"**
2. Дождитесь завершения сборки (3-5 минут)
3. После успешного деплоя получите URL вашего приложения

### Шаг 6: Проверка работоспособности

```bash
# Проверка health endpoint
curl https://ваше-приложение.timeweb.cloud/health

# Ожидаемый ответ:
# {"status":"ok","timestamp":"...","uptime":...,"database":"connected"}

# Проверка статистики (требует API ключ)
curl "https://ваше-приложение.timeweb.cloud/api/admin/stats?key=ВАШ_ADMIN_API_KEY"
```

---

## 🔧 Локальная разработка

### С локальным PostgreSQL:

1. Установите PostgreSQL или запустите через Docker:
   ```bash
   docker run -d --name gwent-postgres \
     -e POSTGRES_DB=gwent \
     -e POSTGRES_USER=gwent_user \
     -e POSTGRES_PASSWORD=dev_password \
     -p 5432:5432 \
     postgres:16-alpine
   ```

2. Создайте `.env` файл:
   ```bash
   cp .env.example .env
   ```

3. Отредактируйте `.env`:
   ```
   NODE_ENV=development
   DATABASE_URL=postgresql://gwent_user:dev_password@localhost:5432/gwent
   DATABASE_SSL=false
   ```

4. Установите зависимости:
   ```bash
   npm install
   ```

5. Запустите сервер:
   ```bash
   npm run dev
   ```

6. Откройте http://localhost:3000

---

## 🤖 Интеграция с Telegram Bot

```python
from telegram import Update, WebAppInfo, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Application, CommandHandler, ContextTypes

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    keyboard = InlineKeyboardMarkup([[
        InlineKeyboardButton(
            "🎮 Открыть турнир",
            web_app=WebAppInfo(url="https://ваше-приложение.timeweb.cloud")
        )
    ]])
    await update.message.reply_text(
        "Добро пожаловать в турнир по Гвинту!",
        reply_markup=keyboard
    )

# Создание бота
app = Application.builder().token("YOUR_BOT_TOKEN").build()
app.add_handler(CommandHandler("start", start))
app.run_polling()
```

---

## 📡 API Endpoints

### Публичные

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/health` | Health check |
| POST | `/api/lobbies` | Создать лобби |
| GET | `/api/lobbies/:code` | Получить лобби по коду |
| PUT | `/api/lobbies/:code/join` | Присоединиться к лобби |

### Административные (требуют `?key=ADMIN_API_KEY`)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/admin/stats` | Статистика сервера |
| GET | `/api/admin/lobbies` | Список активных лобби |
| POST | `/api/admin/cleanup` | Удалить старые лобби |

---

## 🔌 Socket Events

### От клиента к серверу

- `join-lobby` - присоединение к комнате лобби
- `start-faction-selection` - начало игры
- `save-selection-progress` - сохранение прогресса выбора
- `confirm-faction-selection` - подтверждение выбора фракций
- `confirm-faction-ban` - подтверждение бана
- `reset-lobby` - сброс лобби для новой игры

### От сервера к клиенту

- `lobby-update` - обновление состояния лобби
- `faction-selection-started` - начало выбора фракций
- `selection-confirmed` - выбор подтверждён
- `phase-changed` - смена фазы игры
- `ban-confirmed` - бан подтверждён
- `ban-phase-ended` - конец фазы банов

---

## 📁 Структура проекта

```
gwent-app/
├── server/
│   ├── server.js              # Точка входа
│   ├── config/
│   │   └── database.js        # Подключение к PostgreSQL
│   ├── controllers/
│   │   └── lobbyController.js # HTTP API логика
│   ├── models/
│   │   ├── index.js
│   │   ├── Lobby.js           # Модель лобби
│   │   └── Match.js           # Модель матча
│   ├── routes/
│   │   ├── lobbyRoutes.js
│   │   └── adminRoutes.js
│   ├── socket/
│   │   └── lobbySocket.js     # WebSocket логика
│   └── db/
│       └── sync.js            # Скрипт синхронизации БД
├── client/
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── package.json
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

---

## ❓ Troubleshooting

### Ошибка подключения к базе данных

1. Проверьте правильность `DATABASE_URL`
2. Убедитесь что `DATABASE_SSL=true` для Timeweb Cloud
3. Проверьте что IP вашего приложения разрешён в настройках БД

### Ошибка `package-lock.json` рассинхронизирован

```bash
rm package-lock.json
npm install
git add package.json package-lock.json
git commit -m "Regenerate package-lock.json"
git push
```

### Health check возвращает ошибку

1. Проверьте логи приложения в Timeweb Cloud
2. Убедитесь что все переменные окружения настроены
3. Проверьте подключение к базе данных

---

## 📄 Лицензия

MIT

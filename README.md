# 🎮 GWENT Tournament App

Турнирное приложение для карточной игры Gwent с поддержкой Telegram Mini Apps.

## Возможности

- ✅ Создание турнирных лобби с уникальным кодом
- ✅ Два игрока на лобби
- ✅ Форматы Bo3 (3 фракции) и Bo5 (4 фракции)
- ✅ Выбор фракций с таймером
- ✅ Фаза банов
- ✅ Автоматический coin flip для определения первого хода
- ✅ Real-time синхронизация через WebSocket
- ✅ Интеграция с Telegram Mini Apps
- ✅ Адаптивный дизайн для мобильных устройств

## Технологии

- **Backend**: Node.js, Express, Socket.IO
- **Database**: MongoDB (Atlas)
- **Frontend**: Vanilla JS, CSS3
- **Deploy**: Docker, Timeweb Cloud

## Быстрый старт

### Локальная разработка

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Создайте файл `.env` на основе `.env.example`
4. Запустите сервер:
   ```bash
   npm run dev
   ```
5. Откройте http://localhost:3000

### Docker

```bash
# Сборка образа
docker build -t gwent-app .

# Запуск контейнера
docker run -p 3000:3000 --env-file .env gwent-app

# Или через docker-compose
docker-compose up -d
```

## Деплой на Timeweb Cloud

### 1. Подготовка

- Создайте аккаунт на [Timeweb Cloud](https://timeweb.cloud)
- Создайте кластер MongoDB Atlas или используйте управляемую БД Timeweb

### 2. Создание приложения

1. Перейдите в App Platform → Create Application
2. Выберите "Docker" как тип деплоя
3. Подключите Git репозиторий
4. Укажите путь к Dockerfile: `./Dockerfile`

### 3. Переменные окружения

Добавьте следующие переменные:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `NODE_ENV` | Окружение | `production` |
| `PORT` | Порт сервера | `3000` |
| `HOST` | Хост | `0.0.0.0` |
| `MONGODB_URI` | Строка подключения к MongoDB | `mongodb+srv://...` |
| `CORS_ORIGIN` | Разрешённые домены | `*` |
| `ADMIN_API_KEY` | Ключ для админ API | `your-secret-key` |

### 4. Деплой

Нажмите "Deploy" и дождитесь успешной сборки.

### 5. Проверка

```bash
# Health check
curl https://your-app.timeweb.cloud/health

# Статистика (требует API ключ)
curl https://your-app.timeweb.cloud/api/admin/stats?key=YOUR_KEY
```

## Интеграция с Telegram Bot

```python
from telegram import WebAppInfo, InlineKeyboardButton, InlineKeyboardMarkup

async def start(update, context):
    keyboard = InlineKeyboardMarkup([[
        InlineKeyboardButton(
            "🎮 Открыть турнир",
            web_app=WebAppInfo(url="https://your-app.timeweb.cloud")
        )
    ]])
    await update.message.reply_text(
        "Добро пожаловать в турнир по Гвинту!",
        reply_markup=keyboard
    )
```

## API Endpoints

### Публичные

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/health` | Health check |
| POST | `/api/lobbies` | Создать лобби |
| GET | `/api/lobbies/:code` | Получить лобби по коду |

### Административные (требуют `?key=ADMIN_API_KEY`)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/admin/stats` | Статистика сервера |
| GET | `/api/admin/lobbies` | Список активных лобби |
| POST | `/api/admin/cleanup` | Удалить старые лобби |

## Socket Events

### От клиента к серверу

- `create-lobby` - создание лобби
- `join-lobby` - присоединение к лобби
- `start-game` - начало игры
- `save-selection-progress` - сохранение прогресса выбора
- `confirm-faction-selection` - подтверждение выбора фракций
- `confirm-ban` - подтверждение бана
- `reset-lobby` - сброс лобби
- `leave-lobby` - выход из лобби

### От сервера к клиенту

- `lobby-created` - лобби создано
- `lobby-joined` - присоединение успешно
- `lobby-update` - обновление состояния лобби
- `start-faction-selection` - начало выбора фракций
- `opponent-selection-progress` - прогресс выбора оппонента
- `selection-confirmed` - выбор подтверждён
- `start-ban-phase` - начало фазы банов
- `ban-confirmed` - бан подтверждён
- `match-results` - результаты матча
- `lobby-reset` - лобби сброшено
- `error` - ошибка

## Структура проекта

```
gwent-app/
├── server/
│   ├── server.js           # Точка входа
│   ├── controllers/
│   │   └── lobbyController.js
│   ├── models/
│   │   ├── Lobby.js
│   │   └── Match.js
│   ├── routes/
│   │   ├── lobbyRoutes.js
│   │   └── adminRoutes.js
│   └── socket/
│       └── lobbySocket.js  # WebSocket логика
├── client/
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── package.json
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

## Лицензия

MIT
